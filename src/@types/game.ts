export type Game = {
  slug: string;
  name: string;
  plateforms: string[];
  price: number;
  special_offer: number;
  quantity: number;
  image_url: string;
  release_date: string;
  physical: boolean;
  description: string;
};
